{
  "name": "naturale-pos",
  "version": "1.3.0",
  "description": "Local-first POS system for healthy market and café",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run build:migrations && bun --watch src/index.ts",
    "dev:frontend": "cd frontend && bun run dev",
    "db:migrate": "bunx prisma migrate dev && bun run build:migrations",
    "db:generate": "bunx prisma generate && bun run build:migrations",
    "db:studio": "bunx prisma studio",
    "build:migrations": "bun scripts/generate-migrations.ts",
    "build:frontend": "cd frontend && bun run build",
    "build:server": "bun run build:migrations && bun build --compile --minify --outfile dist/pos src/index.ts",
    "package": "bun scripts/package.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.8.0",
    "@prisma/client": "7",
    "hono": "^4.4.7",
    "prisma-adapter-bun-sqlite": "^0.8.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "bun-types": "^1.1.17",
    "prisma": "7",
    "typescript": "^5.5.2"
  }
}
